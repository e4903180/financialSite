/// <reference types="react" />
import { UseTokenProps } from 'react-bootstrap-typeahead/types/behaviors/token';
export interface TokenProps<T> extends UseTokenProps<T> {
    disabled?: boolean;
    readOnly?: boolean;
}
declare const Token: ({ children, option, readOnly, ...props }: TokenProps<HTMLElement>) => JSX.Element;
export default Token;
