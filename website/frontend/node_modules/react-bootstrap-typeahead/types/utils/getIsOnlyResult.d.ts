import { AllowNew, Option } from 'react-bootstrap-typeahead/types/types';
interface Props {
    allowNew: AllowNew;
    highlightOnlyResult: boolean;
    results: Option[];
}
declare function getIsOnlyResult(props: Props): boolean;
export default getIsOnlyResult;
