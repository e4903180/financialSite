import type { Option } from 'react-bootstrap-typeahead/types/types';
declare type Key = 'ArrowDown' | 'ArrowUp';
export declare function isDisabledOption(index: number, items: Option[]): boolean;
export declare function skipDisabledOptions(currentIndex: number, key: Key, items: Option[]): number;
export default function getUpdatedActiveIndex(currentIndex: number, key: Key, items: Option[]): number;
export {};
